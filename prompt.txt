Я хочу улучшить генератор постов, заменив шаблонные тексты на реальный ИИ через Agnes AI.

Мне нужно, чтобы при нажатии кнопки «Сгенерировать» приложение отправляло запрос к Agnes AI API и получало уникальные посты на заданную тему.

Вот что нужно сделать:

1. Создать файл .env в корне проекта и добавить туда переменную:
   VITE_AGNES_API_KEY=sk-SrGTwIJSPu8YTlcRbYAw1QQAWMb7LujN3F6lGg39952suKZv

2. Обновить App.jsx: при клике на кнопку генерировать отправлять POST-запрос к апи Agnes AI. Использовать fetch или axios.

3. Формат запроса:
   URL: https://apihub.agnes-ai.com/v1/chat/completions
   Метод: POST
   Заголовки: 
     - Content-Type: application/json
     - Authorization: Bearer {ключ из .env}
   Тело:
     {
       model: "agnes-2.0-flash",
       messages: [
         { role: "system", content: "Ты — эксперт по написанию постов для Telegram. Ты генерируешь посты на любую тему. Пост должен быть интересным, полезным, содержать заголовок и 3-4 абзаца текста. Твои тексты грамотные, без ошибок, привлекательные. Формат ответа: массив из 5 объектов, каждый с полями title и text (массив строк)." },
         { role: "user", content: `Напиши 5 постов на тему "${topic}"` }
       ],
       temperature: 0.8,
       max_tokens: 1000
     }

4. Обработать ответ: взять массив постов из ответа и отобразить их в карточках как раньше. Если ошибка — показать уведомление «Не удалось сгенерировать, попробуйте позже».

5. Добавить в .gitignore файл .env, чтобы ключ не попал на GitHub.

6. Для работы с переменной окружения используй import.meta.env.VITE_AGNES_API_KEY.

Измени файлы: .env (создать), .gitignore (добавить строку .env), App.jsx (вся логика), generator.js (можно удалить старый код или оставить как fallback). Сделай так, чтобы проект запускался и работал с реальным ИИ.